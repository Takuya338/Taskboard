            <!--テキスト入力フォーム-->
            <div class="form-group">
              <label for="{{ $name }}">{{ $label }}</label>
              <input
                type="name"
                class="form-control"
                name="{{ $name }}"
                id="{{ $name }}"
                size="60"
                placeholder="{{ $placeholder }}"
                value="{{ $value }}" />
            </div>
            