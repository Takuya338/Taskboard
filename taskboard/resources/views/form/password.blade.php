            <!--パスワード入力フォーム-->
            <div class="form-group">
              <label for="{{ $name }}">{{ $label }}</label>
              <input
                type="password"
                class="form-control"
                name="{{ $name }}"
                id="{{ $name }}"
                size="50"
                placeholder="{{ $placeholder }}" />
            </div>